== README

This app is essentially a model-only skeleton of a Bit.ly-type app. It takes in users and their submitted URLs and returns shortened versions. It also has features such as topic tags, premium users, and custom URLs.
